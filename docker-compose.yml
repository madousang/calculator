version: "3"
services:
   calculator:
          build: .
          ports:
             - "8081:8081"
          depends_on:
             - redis
          environment:
             - REDIS_HOST=redis
             - REDIS_PORT=6379
   redis:
        image: redis:latest
