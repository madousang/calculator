version: '3'
services:
  calculator:
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - SPRING_REDIS_HOST=redis
    
  redis:
    image: redis:latest
    ports:
       - "6379:6379"     