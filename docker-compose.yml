version: '3'
services:
  calculator:
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    networks:
      - app-network
    
  redis:
    image: redis:latest
    ports:
       - "6379:6379"     
    networks:
      - app-network

networks:
  app-network:
    driver: bridge